rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default read/write for application entities
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
